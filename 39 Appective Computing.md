
# 情感计算(识别)
多分类问题，把一个句子归类为多个情绪中的一个。  

### 特征提取
* tfidf
* 词性
* n-gram

### 交叉验证
交叉验证和验证数据用于选择最好的超参数  
把训练数据分成两块，一块叫训练数据，另一块叫验证数据  
若CV为5，把训练数据分成5个模块，其中4个是训练数据，1个是验证数据
验证数据依次取5个模块，有5种情形
在这5种情形分别在训练数据上训练模型，然后在对应的验证集上验证，得到5个正确率  
最后把这5个正确率求算术平均值
这是针对一个λ的情况，依次取λ的各种不同情况，分别执行上述操作，最后找到对应最高平均正确率的λ  
