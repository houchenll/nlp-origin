
## 方向导数
偏导数反映的是函数沿坐标轴方向的变化率。  
方向导数反映的是函数沿任一方向的变化率。  


## 梯度
定义：梯度为函数在各坐标轴方向上的偏导数组成的向量  
记作：grad f(x,y) = f_x(x,y)i + f_y(x,y)j  

性质：
* 当方向与梯度方向相同时，函数增加最快。函数在这个方向的方向导数达到最大值，这个最大值就是梯度的模。即，函数在一点的梯度是这样一个向量，它的方向是函数在这点的方向导数取得最大值的方向，它的模就等于方向导数的最大值。
